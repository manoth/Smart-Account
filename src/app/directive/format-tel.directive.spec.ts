import { FormatTelDirective } from './format-tel.directive';

describe('FormatTelDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatTelDirective();
    expect(directive).toBeTruthy();
  });
});
