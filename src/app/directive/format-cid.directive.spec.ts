import { FormatCidDirective } from './format-cid.directive';

describe('FormatCidDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatCidDirective();
    expect(directive).toBeTruthy();
  });
});
